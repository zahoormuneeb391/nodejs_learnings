<!--! You can also Restart you server automatically without using Nodemon -->

## node --watch (introduce in Nodejs v18 in (2022))

using `node --watch <filename>`
instead of `nodemon <filename>` or `node <filename>`
